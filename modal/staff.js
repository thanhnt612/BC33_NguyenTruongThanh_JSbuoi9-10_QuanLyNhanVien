function Staff(account, name, email, day, position, salary, hour, password) {
  this.account = account;
  this.name = name;
  this.email = email;
  this.day = day;
  this.position = position;
  this.salary = salary;
  this.hour = hour;
  this.password = password;
  // this.calcBoss = function () {
  //   return Number(this.salary) * 3;
  // };
  // this.calcManager = function () {
  //   return Number(this.salary) * 2;
  // };
  // this.calcMember = function () {
  //   return Number(this.salary) * 1;
  // };
}

